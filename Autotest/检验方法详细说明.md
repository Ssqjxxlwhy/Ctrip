# 检验方法详细说明文档

## 📋 文档目的

本文档提供35个自动化测试任务的完整检验逻辑说明，确保所有检验脚本严格按照 `任务设计&检验逻辑.xlsx` 中的要求实现。

---

## 🔧 重要更新说明（2025-11-04）

### 修正内容

1. **任务8-22**: 移除了不必要的 `list_shown` 字段检查
   - **原因**: Excel设计中没有要求检查 `list_shown` 字段
   - **影响**: 这些任务现在只检查必要的搜索参数字段

2. **任务23-25**: 完全重写，严格按照Excel设计
   - **任务23**: 上海价格最低酒店搜索（原错误：火车票筛选动车）
   - **任务24**: 机票价格平均值计算（原错误：火车票筛选一等座）
   - **任务25**: 火车票时间范围搜索（原错误：火车票筛选二等座）

3. **任务26-35**: 完全重写，使用booking_history.json
   - 所有预订任务现在检查完整的预订参数
   - 支持复杂的多步骤预订和批量预订验证

---

## 📊 检验逻辑对照表

### 任务1-7：点击操作检验

| 任务号 | 任务描述 | 检验函数 | 数据源 | 检查条件 |
|--------|----------|----------|--------|----------|
| 1 | 点击"酒店"图标 | `check_click_hotel()` | click_history.json | 最新记录="点击'酒店'图标并进入酒店预订页面" |
| 2 | 点击"机票"图标 | `check_click_flight()` | click_history.json | 最新记录="点击'机票'图标并进入机票预订页面" |
| 3 | 点击"火车票"图标 | `check_click_train()` | click_history.json | 最新记录="点击'火车票'图标并进入火车票预订页面" |
| 4 | 点击"消息"按钮 | `check_click_message()` | click_history.json | 最新记录="点击'消息'按钮并进入消息页面" |
| 5 | 点击"行程"按钮 | `check_click_itinerary()` | click_history.json | 最新记录="点击'行程'按钮并进入行程页面" |
| 6 | 点击"我的"按钮 | `check_click_profile()` | click_history.json | 最新记录="点击'我的'按钮并进入我的页面" |
| 7 | 酒店搜索-上海 | `check_hotel_search_chengdu()` | search_params.json | type="hotel_search", city="上海" |

### 任务8-25：搜索操作检验

#### 酒店搜索（8-9, 16-17, 22-23）

| 任务号 | 任务描述 | 检验函数 | 检查条件 |
|--------|----------|----------|----------|
| 8 | 酒店-日期选择 | `check_hotel_search_dates()` | type="hotel_search"<br>checkIn="2025-10-20"<br>checkOut="2025-10-21" |
| 9 | 酒店-房间人数 | `check_hotel_search_guests()` | type="hotel_search"<br>rooms=1, adults=1, children=1 |
| 16 | 酒店-北京日期 | `check_hotel_search_beijing()` | type="hotel_search"<br>city="北京"<br>checkIn="2025-10-22"<br>checkOut="2025-10-23" |
| 17 | 酒店-上海多人 | `check_hotel_search_shanghai()` | type="hotel_search"<br>city="上海"<br>rooms=2, adults=2, children=0<br>（日期动态，不检查） |
| 22 | 酒店-评分排序 | `check_hotel_search_beijing_top5()` | type="hotel_search"<br>city="北京"<br>sortBy="rating"<br>limit=5 |
| 23 | 酒店-价格排序 | `check_hotel_search_shanghai_cheapest()` | type="hotel_search"<br>city="上海"<br>sortBy="price_asc" |

#### 机票搜索（10-12, 18-19, 24）

| 任务号 | 任务描述 | 检验函数 | 检查条件 |
|--------|----------|----------|----------|
| 10 | 机票-广州深圳 | `check_flight_search_gz_sz()` | type="flight_search"<br>from="广州", to="深圳" |
| 11 | 机票-日期选择 | `check_flight_search_date()` | type="flight_search"<br>date="2025-10-22" |
| 12 | 机票-舱位选择 | `check_flight_search_cabin()` | type="flight_search"<br>cabin ∈ ["经济舱","商务舱","头等舱"] |
| 18 | 机票-北京深圳 | `check_flight_search_bj_sz()` | type="flight_search"<br>from="北京", to="深圳"<br>date="2025-10-25" |
| 19 | 机票-成都上海头等舱 | `check_flight_search_cd_sh_first()` | type="flight_search"<br>from="成都", to="上海"<br>date="2025-10-20"<br>cabin="头等舱" |
| 24 | 机票-价格计算 | `check_flight_search_price_avg()` | type="flight_search"<br>from="北京", to="广州"<br>date="2025-10-21"<br>calculation="average_price_top3" |

#### 火车票搜索（13-15, 20-21, 25）

| 任务号 | 任务描述 | 检验函数 | 检查条件 |
|--------|----------|----------|----------|
| 13 | 火车票-北京上海 | `check_train_search_bj_sh()` | type="train_search"<br>from="北京", to="上海" |
| 14 | 火车票-日期选择 | `check_train_search_date()` | type="train_search"<br>date="2025-10-24" |
| 15 | 火车票-学生票 | `check_train_search_student()` | type="train_search"<br>ticketType="学生票" |
| 20 | 火车票-北京上海带日期 | `check_train_search_bj_sh_date()` | type="train_search"<br>from="北京", to="上海"<br>date="2025-10-22" |
| 21 | 火车票-长沙天津学生票 | `check_train_search_cs_tj_student()` | type="train_search"<br>from="长沙", to="天津"<br>date="2026-01-18"<br>ticketType="学生票" |
| 25 | 火车票-时间范围 | `check_train_search_time_range()` | type="train_search"<br>from="广州", to="杭州"<br>date="2025-10-22"<br>timeRange="14:00-17:00" |

### 任务26-35：预订操作检验

#### 简单预订（26-30）

| 任务号 | 任务描述 | 检验函数 | 检查条件 |
|--------|----------|----------|----------|
| 26 | 酒店预订-上海第一家 | `check_booking_hotel_shanghai()` | type="hotel_booking"<br>city="上海"<br>checkIn="2025-10-22"<br>checkOut="2025-10-25"<br>hotelIndex=0, roomIndex=0 |
| 27 | 机票预订-武汉深圳 | `check_booking_flight_wh_sz()` | type="flight_booking"<br>from="武汉", to="深圳"<br>date="2025-11-10"<br>flightIndex=0 |
| 28 | 火车票预订-北京上海 | `check_booking_train_bj_sh()` | type="train_booking"<br>from="北京", to="上海"<br>date="2025-10-23"<br>trainIndex=0 |
| 29 | 酒店预订-上海最便宜 | `check_booking_hotel_cheapest()` | type="hotel_booking"<br>city="上海"<br>checkIn="2025-10-21"<br>checkOut="2025-10-25"<br>selection="cheapest" |
| 30 | 火车票预订-时间范围 | `check_booking_train_time()` | type="train_booking"<br>from="北京", to="上海"<br>date="2025-10-20"<br>timeRange="15:00-16:00" |

#### 复杂预订（31-35）

| 任务号 | 任务描述 | 检验函数 | 检查条件 |
|--------|----------|----------|----------|
| 31 | 多步骤预订 | `check_booking_multi_step()` | type="multi_booking"<br>steps[0]: flight_booking (北京→上海)<br>steps[1]: hotel_booking (上海)<br>steps[2]: train_booking (上海→北京)<br>验证顺序 |
| 32 | 复杂预订+预算检查 | `check_booking_complex_budget()` | type="complex_booking"<br>budgetCheck=2000<br>totalCost存在<br>包含：train(杭州→北京,<=5h)<br>hotel(中关村亚朵,2晚)<br>train(北京→杭州,最晚) |
| 33 | 批量预订-火车票 | `check_booking_batch_train()` | type="batch_booking"<br>bookingType="train"<br>from="深圳", to="北京"<br>date="2025-10-20"<br>quantity=5<br>constraint="duration<=5h" |
| 34 | 复杂批量预订 | `check_booking_complex_batch()` | type="complex_batch_booking"<br>trainBooking: 广州→北京, 5张<br>hotelBooking: 北京, 3家 |
| 35 | 批量预订-机票 | `check_booking_batch_flight()` | type="batch_booking"<br>bookingType="flight"<br>from="北京", to="上海"<br>date="2025-10-15"<br>cabin="经济舱"<br>quantity=6 |

---

## 🔍 检验逻辑实现细节

### 通用检验模式

所有检验脚本都遵循以下标准模式：

```python
def check_xxx():
    # 1. 配置应用包名和文件路径
    app_package = "com.example.Ctrip"
    phone_file_path = "files/xxx.json"

    # 2. 获取ADB路径
    adb_path = os.path.join(
        os.environ.get('ANDROID_HOME', 'C:\\Users\\wudel\\AppData\\Local\\Android\\Sdk'),
        'platform-tools', 'adb.exe'
    )

    # 3. 通过ADB读取文件
    result = subprocess.run(
        [adb_path, "exec-out", "run-as", app_package, "cat", phone_file_path],
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        universal_newlines=True,
        encoding='utf-8',
        check=True
    )

    # 4. 解析JSON
    data = json.loads(result.stdout)

    # 5. 检查最新记录
    latest_event = data["xxx_events"][-1]

    # 6. 验证字段
    return (latest_event.get("field1") == "value1" and
            latest_event.get("field2") == "value2")
```

### 特殊检验逻辑

#### 1. 任务12：舱位选择（多选一）

```python
cabin = latest_event.get("cabin", "")
return (latest_event.get("type") == "flight_search" and
        cabin in ["经济舱", "商务舱", "头等舱"])
```

#### 2. 任务17：动态日期（不检查日期）

```python
# 不检查checkIn和checkOut字段
return (latest_event.get("type") == "hotel_search" and
        latest_event.get("city") == "上海" and
        latest_event.get("rooms") == 2 and
        latest_event.get("adults") == 2 and
        latest_event.get("children") == 0)
```

#### 3. 任务31：多步骤顺序验证

```python
steps = latest_event.get("steps", [])
if len(steps) != 3:
    return False

# 验证第1步
if not (steps[0].get("type") == "flight_booking" and
        steps[0].get("from") == "北京" and
        steps[0].get("to") == "上海"):
    return False

# 验证第2步
if not (steps[1].get("type") == "hotel_booking" and
        steps[1].get("city") == "上海"):
    return False

# 验证第3步
if not (steps[2].get("type") == "train_booking" and
        steps[2].get("from") == "上海" and
        steps[2].get("to") == "北京"):
    return False

return True
```

#### 4. 任务32：预算检查

```python
# 检查budgetCheck字段
if latest_event.get("budgetCheck") != 2000:
    return False

# 检查totalCost字段存在
if "totalCost" not in latest_event:
    return False

# 检查包含的预订组件
steps = latest_event.get("steps", [])
has_train_hz_bj = False
has_hotel_zgc = False
has_train_bj_hz = False

for step in steps:
    if (step.get("type") == "train_booking" and
        step.get("from") == "杭州" and
        step.get("to") == "北京"):
        has_train_hz_bj = True
    elif (step.get("type") == "hotel_booking" and
          "中关村" in step.get("hotelName", "") and
          "亚朵" in step.get("hotelName", "")):
        has_hotel_zgc = True
    elif (step.get("type") == "train_booking" and
          step.get("from") == "北京" and
          step.get("to") == "杭州"):
        has_train_bj_hz = True

return has_train_hz_bj and has_hotel_zgc and has_train_bj_hz
```

---

## 📝 JSON数据结构规范

### click_history.json

```json
{
  "clicks": [
    "点击'酒店'图标并进入酒店预订页面",
    "点击'机票'图标并进入机票预订页面"
  ]
}
```

### search_params.json

```json
{
  "search_events": [
    {
      "type": "hotel_search",
      "city": "上海",
      "checkIn": "2025-10-20",
      "checkOut": "2025-10-21",
      "rooms": 1,
      "adults": 1,
      "children": 0,
      "sortBy": "price_asc",
      "limit": 5
    },
    {
      "type": "flight_search",
      "from": "北京",
      "to": "上海",
      "date": "2025-10-22",
      "cabin": "经济舱",
      "calculation": "average_price_top3"
    },
    {
      "type": "train_search",
      "from": "广州",
      "to": "深圳",
      "date": "2025-10-23",
      "ticketType": "学生票",
      "timeRange": "14:00-17:00"
    }
  ]
}
```

### booking_history.json

```json
{
  "booking_events": [
    {
      "type": "hotel_booking",
      "city": "上海",
      "checkIn": "2025-10-22",
      "checkOut": "2025-10-25",
      "hotelIndex": 0,
      "roomIndex": 0,
      "selection": "cheapest"
    },
    {
      "type": "flight_booking",
      "from": "武汉",
      "to": "深圳",
      "date": "2025-11-10",
      "flightIndex": 0
    },
    {
      "type": "train_booking",
      "from": "北京",
      "to": "上海",
      "date": "2025-10-23",
      "trainIndex": 0,
      "timeRange": "15:00-16:00"
    },
    {
      "type": "multi_booking",
      "steps": [
        {
          "type": "flight_booking",
          "from": "北京",
          "to": "上海"
        },
        {
          "type": "hotel_booking",
          "city": "上海"
        },
        {
          "type": "train_booking",
          "from": "上海",
          "to": "北京"
        }
      ]
    },
    {
      "type": "complex_booking",
      "budgetCheck": 2000,
      "totalCost": 1850,
      "steps": [
        {
          "type": "train_booking",
          "from": "杭州",
          "to": "北京",
          "duration": 4.5
        },
        {
          "type": "hotel_booking",
          "hotelName": "北京中关村亚朵酒店",
          "nights": 2
        },
        {
          "type": "train_booking",
          "from": "北京",
          "to": "杭州",
          "time": "latest"
        }
      ]
    },
    {
      "type": "batch_booking",
      "bookingType": "train",
      "from": "深圳",
      "to": "北京",
      "date": "2025-10-20",
      "quantity": 5,
      "constraint": "duration<=5h"
    },
    {
      "type": "complex_batch_booking",
      "trainBooking": {
        "from": "广州",
        "to": "北京",
        "quantity": 5,
        "time": "earliest"
      },
      "hotelBooking": {
        "city": "北京",
        "quantity": 3,
        "different": true
      }
    },
    {
      "type": "batch_booking",
      "bookingType": "flight",
      "from": "北京",
      "to": "上海",
      "date": "2025-10-15",
      "cabin": "经济舱",
      "quantity": 6
    }
  ]
}
```

---

## ⚙️ 使用示例

### 运行单个测试

```bash
cd Autotest
python eval_8.py
```

**输出**:
- `true` - 测试通过
- `false` - 测试失败

### 批量运行测试

```bash
cd Autotest
for i in {8..35}; do
    echo "任务 $i: $(python eval_$i.py)"
done
```

### 在Python代码中调用

```python
import sys
sys.path.append('Autotest')
from eval_8 import check_hotel_search_dates

result = check_hotel_search_dates()
print(f"任务8检验结果: {'通过' if result else '失败'}")
```

---

## 🛠️ Android集成示例

### 记录搜索参数（任务8示例）

```kotlin
// 在HotelListTabCompose.kt中
SearchParamsManager.recordHotelSearch(
    context = context,
    city = selectedCity,
    checkIn = "2025-10-20",
    checkOut = "2025-10-21",
    rooms = rooms,
    adults = adults,
    children = children,
    listShown = true  // 可选字段，不影响检验
)
```

### 记录预订操作（任务26示例）

```kotlin
// 点击酒店预订按钮时
BookingHistoryManager.recordHotelBooking(
    context = context,
    city = "上海",
    checkIn = "2025-10-22",
    checkOut = "2025-10-25",
    hotelIndex = 0,
    roomIndex = 0
)
```

---

## ✅ 检验清单

使用此清单确保所有检验脚本正确实现：

- [x] 任务1-7: 点击操作检验（click_history.json）
- [x] 任务8-22: 搜索操作检验（search_params.json，无list_shown）
- [x] 任务23-25: 高级搜索检验（search_params.json）
- [x] 任务26-30: 简单预订检验（booking_history.json）
- [x] 任务31-35: 复杂预订检验（booking_history.json）
- [x] 所有脚本语法验证通过
- [x] 严格按照Excel设计实现

---

## 📞 技术支持

### 常见问题

**Q: 为什么任务8-22不再检查list_shown？**
A: Excel设计文档中只要求检查特定的搜索参数字段，list_shown不是必需的检查项。

**Q: 任务17为什么不检查日期？**
A: 根据Excel设计，任务17的日期是动态的（"今天"和"明天"），因此只检查房间数和人数。

**Q: 如何测试复杂预订任务（31-35）？**
A: 这些任务需要在应用中执行复杂的多步骤操作，确保BookingHistoryManager正确记录所有步骤。

---

**版本**: 2.0
**创建日期**: 2025-11-04
**最后更新**: 2025-11-04
**更新内容**: 修正所有检验逻辑，严格按照Excel设计实现
